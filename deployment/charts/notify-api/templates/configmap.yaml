kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    name: {{ .Chart.Name }}
data:
  EMAIL_PROVIDER: {{ .Values.configMapData.EMAIL_PROVIDER }}